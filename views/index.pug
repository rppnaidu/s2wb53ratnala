extends layout

block content
  h1= title
  p Welcome to #{title}
  p Given this paragraph for reference
  
  - var btnType = 'danger'
  - var btnSize = 'sm'
  button(type='button' class='btn btn-' + btnType + ' btn-' + btnSize) press me